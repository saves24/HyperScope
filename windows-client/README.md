# Windows Desktop Client (future)

Planned Tauri desktop application for HyperScope on Windows.

- No listening port: it connects out to nodes over their API (hyper-node :5000)
- Uses the shared business core (../core) — same logic as the Linux web panel
- Planned: multi-node dashboard, add/manage nodes, history/events

Not yet implemented — placeholder for the Windows-specific code (Tauri shell + Windows UI).
