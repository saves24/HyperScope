plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("org.jetbrains.kotlin.plugin.serialization")
}

android {
    namespace = "com.hyperscope.android"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.hyperscope.android"
        minSdk = 28
        targetSdk = 34
        versionCode = 1
        versionName = "1.0.0"
    }

    signingConfigs {
        // Debug: use the standard Android debug keystore (auto-generated by AGP).
        getByName("debug") {
            storeFile = file("../keystore/debug.p12").takeIf { it.exists() }
                ?: File(System.getProperty("user.home"), ".android/debug.keystore")
            storePassword = System.getenv("DEBUG_KEYSTORE_PASSWORD") ?: "android"
            keyAlias = "androiddebugkey"
            keyPassword = System.getenv("DEBUG_KEYSTORE_PASSWORD") ?: "android"
        }
        // Release: keystore is NOT in the repo. CI decodes it from the
        // RELEASE_KEYSTORE secret into ../keystore/release.p12 and exports the
        // password via RELEASE_KEYSTORE_PASSWORD. Local release builds require
        // the same setup.
        create("release") {
            storeFile = file("../keystore/release.p12").takeIf { it.exists() }
                ?: File(System.getProperty("user.home"), ".hyperkeys/release.p12")
            storePassword = System.getenv("RELEASE_KEYSTORE_PASSWORD") ?: "hyperscope"
            keyAlias = "hyperscope"
            keyPassword = System.getenv("RELEASE_KEYSTORE_PASSWORD") ?: "hyperscope"
        }
    }

    buildTypes {
        release {
            isMinifyEnabled = false
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
            signingConfig = signingConfigs.getByName("release")
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
    composeOptions {
        kotlinCompilerExtensionVersion = "1.5.10"
    }
    buildFeatures {
        compose = true
    }
}

dependencies {
    val composeBom = platform("androidx.compose:compose-bom:2024.02.01")
    implementation(composeBom)
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.activity:activity-compose:1.8.2")
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-tooling-preview")
    implementation("androidx.compose.material3:material3")
    implementation("androidx.compose.material:material-icons-core")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
    implementation("androidx.lifecycle:lifecycle-runtime-compose:2.7.0")
    implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.7.0")
    implementation("androidx.navigation:navigation-compose:2.7.7")
    implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.2")
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("androidx.datastore:datastore-preferences:1.0.0")
    debugImplementation("androidx.compose.ui:ui-tooling")
    testImplementation("junit:junit:4.13.2")
}
